# Development Roadmap

## Current status snapshot

Last refreshed: 2026-06-11 21:14 UTC.

- Active work is still the desktop GUI artifact gate for release `0.1.36-dev`; Darwin/macOS Wails outputs remain the release blocker.
- Docker/web-GUI quick slice completed in the working tree: runtime image is Alpine-based instead of full Debian, container bundle now hosts a "Development in progress" engine-status page, Docker exposes web GUI HTTP port `8385` plus default HTTPS port `8943`, first-run config writes `webGUI.httpsListen` when `FSE_WEB_GUI_TLS_ENABLED=true`, Docker variables can override the web GUI HTTP listener, HTTPS toggle, and HTTPS listener, the web GUI server can auto-generate a local self-signed certificate/key under the install directory, checksum is refreshed, and runtime defaults are Unraid-friendly `PUID=99`, `PGID=100`, `FSE_UMASK=002`.
- Real Wails outputs already verified: `linux-amd64`, `linux-arm64`, `windows-amd64`, and `windows-arm64`.
- Linux desktop installers already verified: `.deb`, `.rpm`, and `.AppImage` for both Linux architectures.
- Remaining release blocker: build and verify `darwin-amd64` and `darwin-arm64` Wails outputs, then run final all-six desktop GUI release packaging.
- Darwin builder image status: the SDK-backed osxcross/Wails builder image completed successfully at `2026-06-11T11:52:29Z`; durable status is `/development/logs/file-sync-desktop-darwin/build-darwin-builder.status`; log is `/development/logs/file-sync-desktop-darwin/build-darwin-builder-20260611T075536Z.log`; image is `fse-desktop-wails-builder:debian12-wails2.10.2-darwin-osxcross`. The Darwin Wails outputs remain missing.
- Current environment handoff: the 2026-06-11 20:14 UTC Darwin Wails attempt proved the osxcross fallback path and the scratch-copy `go mod tidy` fix, then failed earlier in the Wails cross-compile step against the MacOSX26 SDK with Objective-C/CoreText/WebKit header parse errors such as missing `CFAttributedStringRef`; because `set -e` exited on the failed `wails build`, the Darwin fallback never ran. The wrapper now treats Darwin Wails cross-compile failure as a fallback trigger, runs the osxcross Go app-bundle fallback, and still fails fast for non-Darwin Wails failures. Focused RED/GREEN contract, `go test ./...`, and `scripts/run-serious-harness.sh` passed. A short 2026-06-11 20:52 UTC foreground retry was interrupted by the 600s cron command timeout before durable status finalization, with no active Docker/Wails/go-build process left afterward; the next pass should start a fresh corrected Darwin-only build with durable log/status and allow it to run unattended.
- Completed Docker/CLI follow-up: first-class `web-gui status|install|update|start|stop` and `identity export|import` CLI commands now parse, dispatch through command/runtime layers, call authenticated daemon-control endpoints with API-key/TLS bootstrap, print daemon responses, and are covered by focused package tests plus serious-harness command coverage.

## Phase 0 — Core contracts in progress

- [x] Go module and daemon entry point.
- [x] JSON configuration model with validation.
- [x] Hot reload manager that rejects invalid configs and preserves last known good config.
- [x] 15-second quiet-period/debounce reload manager so partial config writes are retried rather than adopted.
- [x] Fixed-block SHA-256 manifests.
- [x] Block-delta planner.
- [x] Content-hash delta planner that reuses identical shifted blocks and blocks from other files/libraries.
- [x] Apply ordering planner: writes before deletes when space allows; deletes early only to free needed space.
- [x] One-way/two-way folder mode policy skeleton.
- [x] Manual-peer discovery source skeleton.
- [x] Local folder scanner.
- [x] Simple JSON metadata store for early tests, including save/load/list/delete manifest operations and content-hash block lookup across folders/files.
- [x] Pipe/stream transport abstraction for externally supplied connections.
- [x] Minimal CLI parser for `start [config]` and `stop [config]`.
- [x] Project rules and implementation plan.
- [x] `status` command shape and authenticated `/v1/status` API query.
- [x] Authenticated realtime API skeleton with `/v1/status` and `/v1/events`.
- [x] Skeleton config generation with JSONC comments and generated API key.
- [x] Atomic config writes for generated configs, API-key persistence, and CLI peer/folder edits.
- [x] Local block assembly primitive: stage file from reusable local blocks, verify manifest, then atomic replace.
- [x] One-way local sync primitive using scanner + staged local apply, with writes before stale deletes.
- [x] Folder sync groups for local prototype routing between configured folders.
- [x] Debounced scan scheduler seam with periodic fallback scans for missed watcher events.
- [x] Recursive fsnotify watcher wrapper for near-real-time local file events.
- [x] Realtime SSE subscriptions stay open for live events, plus `/v1/folders` and `/v1/peers` state endpoints.
- [x] Prototype peer data endpoints: authenticated folder index, file download, and single-block download.
- [x] `config show` redacts API keys instead of printing secrets.
- [x] CLI config mutation basics: peer/folder list/add/remove.
- [x] Build script for Linux/macOS/Windows amd64+arm64 artifacts under `build/<version>/`.
- [x] Initial transport-agnostic peer protocol codec for stream/pipe message contracts.

## Phase 1 — Functional local sync loop

- [x] Connect config folders to scanner and local apply primitive for one-way local sync.
- [x] Add metadata-only quick folder indexing pass: path, size, mtime, ctime/birth time where available, unknown block-hash state.
- [x] Add lazy hash queue that hashes files/blocks on demand and during idle background work instead of blocking folder adoption.
- [x] Add manual seed adoption flow: provisionally trust authoritative peer index when local quick metadata matches, then verify/repair lazily and correct dates/metadata to authoritative DB values if unchanged since baseline.
- [x] Add API read-only/provisional hashing state for seeded folders/files, date/metadata correction pending state, and hash/repair progress counters/events.
- [x] Debounced filesystem watching scheduler logic.
- [x] Periodic full scan fallback scheduler logic.
- [x] Local two-folder integration test for sendonly/recvonly behavior.
- [x] Wire scheduler and local sync primitive into daemon runtime.
- [x] Local two-folder integration test for sendrecv conflict behavior.
- [x] Add real filesystem watcher backend.
- [x] Wire fsnotify backend events into scheduler and API events in daemon runtime for initial startup config.
- [x] Rebuild folder monitors on hot config reload when folder set changes.
- [x] Conflict file naming and preservation.
- [x] Config model accepts and validates folder-level permission policy shape: ignore, sync, default, fixed plus optional file/dir modes and owner/group/ACL flags.
- [x] Implement apply-time permission policy behavior: ignore leaves permissions untouched, sync copies source file mode, default/fixed apply configured file/dir modes after verified local apply.
- [x] Restart recovery for interrupted staged writes and incomplete files.
- [x] Syncthing-compatible ignore support using `.sync/ignore`: scanner now skips `.sync/` metadata, applies primary ignore-file basic glob/comment/`!` include rules on each scan, loads local `#include` files relative to the share root, supports recursive `**` matches, bracket character classes, escaped literal glob characters, distinguishes anchored `/dir/` directory rules from unanchored `dir/` directory rules, honors `(?i)` case-insensitive rules, requires exact `#include` directives instead of treating ordinary comments as includes, preserves last-match ordering across included files, stream pull stale-delete planning preserves `.sync/` metadata plus locally ignored paths, fetches missing in-share includes before requesting the normal folder index, and reports unavailable include paths in the pull result, manual HTTP peer pulls fetch missing in-share include files before planning, report unavailable include paths in peer finished events, and skip locally ignored remote paths before transfer/write/delete planning, and local folder sync skips target-ignored writes while keeping ignored target files out of stale-delete/block-reuse planning.
- [x] Local one-way exact move detection: when a source file appears at a new path and a stale target file has an identical manifest, rename the stale target file into place before stale-delete planning instead of copying and deleting it.
- [x] Scanner inaccessible-file reporting and pending locked-apply cache foundation: per-file read/open failures are returned as inaccessible path warnings while the scan continues indexing other files, quick indexing preserves the prior manifest/index record for inaccessible paths instead of pruning them as deletes, verified pending apply block bytes for write-locked targets can be stored under `.sync/locked-apply/` with matching pending-write metadata, pending locked applies can be retried after restart when the current target still matches the recorded expected base, a changed locked target is preserved as an extension-preserving conflict copy before the desired manifest is applied, and compact locked/inaccessible warning state is exposed through folder API status plus realtime `folder.warning` events.

## Phase 2 — Peer transport

- [x] Prototype manual HTTP peer pull path between daemon processes using folder index + block download.
- [x] Define initial internal protocol message envelope for hello, folder index, block request, block response, apply result, and error over newline-delimited stream JSON.
- [x] Implement prototype peer data transfer over pipe/stdio or externally supplied streams using the initial stream protocol, including block verification, local block reuse, and stdin/stdout CLI wiring; still needs production hardening, resumability, and richer config integration.
- [x] Replace prototype whole-file peer pulls with verified per-block requests for changed files.
- [x] Add missing-block planning so peers skip blocks already reusable from local files.
- [x] Add encrypted/authenticated peer identity with configurable encryption level 0-10.
- [x] Add peer encryption negotiation: default to the highest level set by either peer, with explicit lawful-compatibility/drop-in weaker modes available by parameter.
- [x] Define encryption level mapping: 0 no encryption/debug visibility; 1 minimal/permissive; 4-5 normal strong/bank-grade; 10 maximum/high-CPU protection.
- [x] Add resumable block transfer for the prototype stream pull path: valid interrupted temp blocks are verified against the manifest and retained, while missing/corrupt blocks are fetched again.
- [x] Add prototype stream-pull exact move reuse: when a peer file appears at a new path and a stale local file has an identical manifest, rename the stale local file into place before block requests and stale-delete planning.
- [x] Add manual HTTP peer-pull exact move reuse: when a peer file appears at a new path and a stale local file has an identical manifest, rename the stale local file into place before block downloads and stale-delete planning.
- [x] Add prototype metadata rename hints: exact moves can be emitted as one cursor-ordered `move` metadata change with `fromPath`, destination path, revision, and destination manifest, and peer-scoped caches apply that move without mutating local manifests.

## Phase 3 — Discovery

- [x] Optional local discovery.
- [x] Prototype trusted peer exchange over stream handshakes.
- [x] Optional identity group config/protocol support plus trusted stream folder-advertisement exchange that returns learned folders as disabled/no-path entries.
- [x] Public DHT discovery using public DHT servers; ship-readiness requirement. Tested source seam, namespace/bootstrap config, daemon discovery-source wiring seam, daemon discovery polling/API-state adoption helper, periodic daemon discovery polling, concrete libp2p/Kademlia router adapter, and runtime injection into the polling loop are in place.
- [x] Config switch to disable all discovery and rely only on configured peers.

## Phase 4 — Durable metadata DB

- [x] Keep store interface stable so tests continue to run against memory/JSON implementation; current interface supports save/load/list/delete manifests and engine stale-manifest cleanup.
- [x] Benchmark embedded DB candidates against sync workloads: Pebble first, then Badger/bbolt fallback; avoid traditional SQLite as default unless proven not to bottleneck concurrent read/write metadata operations. Label current-host results as slow/resource-constrained low-end stress data and repeat finalists on other hardware before final selection. First small runnable harness and same-host report are in `cmd/fse-metabench`, `internal/metabench`, and `docs/METADATA_DB_BENCHMARK_2026-05-21.md`; `scripts/make-external-metabench-bundle.sh` now builds six-target `fse-metabench` bundles with host-fact/result return runners for better-hardware confirmation. Backend selection is still pending returned broader/larger external runs.
- [x] Replace JSON store with the selected high-concurrency embedded DB backend. Current progress: the state package now has a snapshot backend seam plus a first Badger-backed durable store; config/runtime selection is wired for the daemon runtime, API state loading, `scan`, `metadata compact`, `metadata import-json`, and snapshot marker CLI/API operations; JSON snapshot import into the configured durable backend backs up the existing target store before replacement; the Badger backend now persists the prototype metadata as key-level records for manifests, revisions, tombstones, rename hints, peer metadata/cache/checkpoints, block lookup indexes, pending writes, skipped deletes, compaction snapshots, and backup/versioning snapshot markers; older whole-snapshot Badger stores are migrated to key-level records on open so direct hot paths work immediately and the legacy snapshot key is removed; hot local Badger manifest APIs now use direct key-level reads/writes for manifest, revision, tombstone, cursor, and block-index records without whole-keyspace rewrites; hot peer metadata/cache/checkpoint Badger APIs now use direct key-level reads/writes for peer folder state, peer-scoped manifests/revisions/tombstones/rename hints, peer cursors, peer apply checkpoints, and full-refresh peer-cache replacement; hot apply-gate/compaction Badger APIs now use direct key-level reads/writes for pending writes, verified staged-block updates, committed pending-write flags, skipped deletes, and compaction snapshots; `metadata.perFolder` now provides a Badger-only prototype layout that lets `scan` write each configured folder into its own physical metadata DB under the configured metadata root, the daemon/API aggregate view can delegate content-hash block lookup across those per-folder stores without creating a synthetic aggregate Badger DB, and `fse metadata split-badger --source <metadata.badger>` migrates a single Badger store into the configured per-folder root with target-root backup/restore safety. Benchmark sign-off and production-default selection are still pending.
- [x] Extend the prototype per-folder metadata cursor/tombstone summary into peer-scoped differential state synchronization. Current JSON store can report deterministic summaries and changes since a cursor, persist peer-scoped folder state vectors/status, expose configured peers' per-folder metadata cursor/hash status through API peer state and folder-level deferred destructive-delete counts through API folder state, round-trip stream protocol `metadata_state`/`metadata_changes`/`metadata_ack` messages for summary/cursor-range exchange, represent exact rename hints as `move` metadata changes, handle server-side stream metadata-state reconciliation by recording remote peer summaries and replying with local changes since their cursor, apply received peer metadata changes into a peer-scoped cache after cursor/hash validation without touching local manifests, persist per-peer/folder apply checkpoints so already committed metadata batches resume as safe no-ops, split prototype stream `metadata_changes` responses into capped deterministic cursor-contiguous batches when configured and require acknowledgement before sending the next chunk, allow the pull side to stop metadata catch-up after a configured batch budget so file/block transfer can proceed, optionally continue the remaining catch-up on a separate metadata-only stream while the foreground stream transfers files/blocks, defer stream-pull stale deletes into the prototype apply/delete gate when foreground metadata catch-up is paused before the peer's current summary, apply persisted skipped-delete gates from runtime reconciliation only after metadata/write prerequisites are satisfied, run active daemon metadata-only reconciliation polling for configured lagging peers over TCP-style stream endpoints, run that peer metadata exchange before prototype stream file/block transfer when a metadata store is configured, skip stream file apply when the local block manifest already matches the peer manifest, persist prototype apply/delete gate records for pending writes, verified staged blocks, committed write prerequisites, and skipped destructive deletes, plan/apply safe prototype tombstone compaction with pre-prune snapshots only up to the known peer-safe cursor, expose a manual `fse metadata compact` trigger for configured folders, and return a `metadata_full_refresh_required` stream error when a peer requests metadata from before a compacted safe cursor, and repair that peer-scoped cache by replacing it from the following full folder index before continuing verified stream file/block transfer. Richer reconciliation controls, production periodic compaction scheduling, and broader durable DB integration are still pending.
- [x] Add low-priority background database maintenance worker that snail-crawls DB state, prunes orphaned block/file/queue/tombstone artifacts, verifies indexes, and yields to foreground sync/API/transfer work. Current progress: prototype `internal/maintenance` provides a scheduled non-blocking worker loop, foreground-yield hook, max file/byte/time budgets, atomic file-backed checkpoint persistence, safe cursor resume/reset after a complete crawl, revision-aware folder/path cursor markers so restarts do not keep rechecking earlier records when new records sort before the saved position, a deterministic manifest crawler over configured folders, an apply-gate crawler that prunes committed pending-write records once no skipped delete still requires that committed write, prunes skipped-delete gates whose required pending-write record is missing, and reports skipped-delete gates whose metadata prerequisite cursor has already been reached with a non-matching state hash while preserving the gate for conservative review, a block-index crawler that reports content-hash index entries whose owner manifest is missing or whose current manifest no longer contains the indexed block, a metadata-consistency crawler that reports live-manifest/revision/tombstone/cursor bookkeeping inconsistencies without pruning so deletes are not resurrected, and a file-scrub crawler that supports full-block, sampled-block, and light-metadata verification modes while reporting missing/unreadable/mismatched files without modifying filesystem bytes. Scrub classification now treats changed metadata as likely local edits, unchanged-metadata block-hash divergence as suspected corruption, missing timestamp evidence as needs-user-review instead of auto-repair input, same-path uncommitted pending writes as pending-write evidence requiring user review instead of corruption labeling, recent watcher/read/checksum error history as needs-user-review evidence, optional repeated-verification evidence gates suspected-corruption escalation until repeated passes observe the same mismatch, and trusted peer/backup consensus evidence either strengthens suspected-corruption reporting or keeps peer-confirmed local divergence in needs-user-review. Suspected-corruption manifests can be marked damaged so their blocks are no longer advertised/reused, repair placement verifies trusted replacement bytes before atomically placing them, original bytes are preserved under `.sync/quarantine/`, repair-loop backoff state blocks repeated failed repairs until delay/trusted-manifest changes, scrub alerting emits folder warnings plus `maintenance.warning` events, and `fse maintenance scrub [--folder <id>] [config-path]` runs the selected configured scrub mode on demand with configured per-run budgets and resumable checkpoints. API/status wiring and daily-budget accounting remain pending.
- [x] Expose DB maintenance status and API manual trigger through API/CLI after durable DB lands.
  - Maintenance scrub alerting now has a tested daemon/API helper that logs compact warnings, publishes `maintenance.warning` events, and includes classification plus quarantine/not-quarantined status in folder warning records. `GET /v1/status` includes compact maintenance status that the `fse status` CLI returns through the existing authenticated daemon API, and `POST /v1/maintenance/scrub` runs the same bounded configured scrub path as `fse maintenance scrub`, updates last-manual-scrub status, and publishes `maintenance.scrub.finished`.

## Phase 5 — Operational daemon

- [x] Structured logs. Maintenance scrub warning logs and core daemon startup/API/config-reload/monitor/public-DHT operational logs are emitted as parseable JSON records via a reusable structured log helper; daemon operational logging configuration/event wrappers now live in a focused `internal/daemonlogging` package instead of the large CLI entrypoint. CLI fatal/error paths use a documented human-readable default policy with concise `fse:` stderr lines and no standard logger timestamp/prefix, while machine-readable automation should use the authenticated daemon API until an explicit CLI JSON mode exists. Daemon config supports hot-reloaded `logging.level` (`debug`/`info`/`warn`/`error`/`off`) plus `logging.output` (`stderr`/`stdout`/config-relative or absolute file path).
- [x] Scriptable CLI commands for validate, scan, status.
- [x] Global and per-peer transfer rate limits. Config accepts and validates global `transfer.sendBytesPerSecond`/`transfer.receiveBytesPerSecond` plus per-peer `sendBytesPerSecond`/`receiveBytesPerSecond`; `0` means disabled/no limit and negative caps are rejected. The transfer-limit evaluator computes local send/receive caps with per-peer override fallback, supports two-sided lowest-nonzero negotiation semantics, and reports whether each effective direction is caused by a local peer cap, local global cap, remote send cap, remote receive cap, or no limit. Prototype stream hello messages advertise each side's local caps for the peer, stream pull records the negotiated caps and cause strings after handshake, and stream block responses/pulls enforce negotiated send/receive caps. Manual HTTP peer pulls enforce the configured local receive cap for fetched blocks. API peer state exposes configured/effective local caps plus cause strings.
- [x] systemd / launchd / Windows service install helpers. Current progress: `fse service render --platform systemd|launchd|windows --binary <path> [--user user] [config-path]` renders reviewable service definitions/scripts with explicit binary/config paths and no embedded secrets; systemd package-manager install/uninstall handoff commands are documented for administrator-owned privileged paths; macOS launchd handoff helpers now render reviewable install/load and unload/uninstall commands for system and user domains; Windows handoff helpers render reviewable elevated PowerShell install/start plus commented stop/uninstall commands with safe argument quoting; `POST /v1/stop` plus `fse stop` now provide a real authenticated graceful daemon stop path after the service manager has started the process; `fse service status|start|stop|restart --platform systemd|launchd|windows --name <service>` renders explicit platform-owned lifecycle command adapters without executing privileged service-manager operations.
- [x] Cross-platform CI matrix. GitHub Actions now runs `go test ./...`, the serious harness, cross-compiles all six required Linux/macOS/Windows amd64+arm64 targets, and smoke-tests `scripts/build-all.sh` for CI artifact coverage while keeping release artifact rules under `build/<version>/`.
- [x] Generate external benchmark/smoke test bundle scripts for Brandon's Mac/Windows/Linux ARM and arranged Linux AMD64/Neko test targets. `scripts/make-external-smoke-bundle.sh` packages the six cross-compiled binaries with Unix and Windows smoke runners that collect host facts, run generated-data config/folder/validate/scan/status/service-render checks, and write returnable JSON/Markdown/log bundles.
- [x] Docker/container core daemon defaults foundation. `Dockerfile` and `scripts/container-entrypoint.sh` build a Linux daemon container with `/config` as the persistent root, first-run generated config under `/config/config.jsonc`, per-folder Badger metadata under `/config/metadata`, logs under `/config/logs`, a reserved `/config/web` state root, `PUID`/`PGID` ownership mapping, and non-secret first-run listener/log/discovery environment defaults. Container images now bundle a minimal default optional web GUI zip under `/opt/fse/web/`, enable it in newly generated container configs by default, and allow headless first-run defaults with `FSE_WEB_GUI_ENABLED=false`. `.github/workflows/container.yml` publishes signed GHCR multi-arch Linux amd64/arm64 images from release tags/manual dispatches with semantic and immutable SHA tags, cosign keyless signing, and documented signature verification. Optional `FSE_IDENTITY_EXPORT_PATH` writes a secret JSON identity package from the current config for explicit pairing/bootstrap import without logging it and without overwriting an existing export unless `FSE_IDENTITY_EXPORT_FORCE=true`. First-run config-default mutation and identity export now run through the Go-owned `fse container-bootstrap` helper, so the runtime image no longer installs Python for entrypoint bootstrap/export work.
- [x] Optional web GUI package trust contract and trusted package lifecycle foundation. Config now includes a disabled-by-default `webGUI` package/source contract with version, install directory, daemon-managed web-server listen address, local package path or HTTPS update URL, and required SHA-256 checksum when enabled; authenticated `/v1/web-gui-command` now reports explicit disabled/headless status when the web GUI is off, installs/updates trusted local or HTTPS zip packages with checksum verification, HTTPS-only remote fetching, zip path safety checks, atomic install-directory replacement, and starts/stops a daemon-owned static web UI server for the installed package with `/health` reporting. Container images bundle a minimal trusted web GUI package and enable it only for first-run container configs by default while preserving headless core-daemon operation for non-container and opt-out container deployments. Rich production web UI assets remain planned.
- Latest backup sync checkpoint: stream identity-group peer exchange now carries snapshot marker metadata plus per-snapshot verified archive-block availability as `archiveFullyProtected` and offline DB-checkpoint availability as `dbCheckpointAvailable`; learned non-backup peers record the availability flags without creating archive jobs or offline checkpoints.
- [x] Future backup/versioned snapshot mode. Snapshot markers, per-revision manifest history, backup destination modes, archive intake, mirror/checkpoint foundations, backup-intake retention/pruning, snapshot availability peer exchange, dry-run restore plans, CLI restore execution, authenticated restore API state/events, a retention safety planner, and an internal/API/CLI retention executor are in place. Retention execution deprecates old unpinned snapshots before deletion, persists inherited-manifest promotions before deleting deprecated marker roots, removes only content-addressed archive blocks that no retained snapshot or current live state references, and now persists durable retention job records with job IDs plus operation counters for API/GUI progress. `fse snapshot restore` and `POST /v1/restores` now execute selected-file/directory/full-snapshot restores from verified archive blocks through staged manifest-verified atomic placement to the original folder root or safe alternate destination, reject unsafe alternate paths before writes, reruns skip destination files that already verify against the snapshot manifest, and persist durable restore job checkpoint records with per-file completion states; restore totals include total/restored/skipped/remaining file counts through CLI output, API responses, `backup.lastRestore`, and `snapshot.restore.finished` events so API/GUI clients can show rerun/resume progress without parsing logs. Backup archive/checkpoint scrub and conservative repair foundations can report and repair verified archive/checkpoint evidence; archive-block repair execution now optionally persists durable repair job records with per-block repaired/unresolved status for resumable API/GUI progress. `GET /v1/backup/jobs` exposes durable restore, retention, and repair operation records for authenticated GUI/API progress views.
- [x] Full encrypted control API. Current encrypted authenticated control surface is in place: config supports `api.encryption.mode` (`auto`, `off`, or `manual-tls`), loopback plaintext remains compatible under `auto`, non-loopback `auto` listeners serve HTTPS and generate a local self-signed certificate/key beside the active config when certificate paths are empty, and manual TLS serves the daemon API with configured certificate/key files. `fse status` and `fse stop` now use HTTPS when API TLS is required, trust the configured/generated certificate file without disabling verification, and optionally pin the daemon API certificate through `api.encryption.trustedCertificateSha256` so mismatched certificates are rejected. Authenticated `GET /v1/api/trust` now reports current API TLS mode/requirement, the served certificate fingerprint, the configured trusted fingerprint, and whether they match, while omitting API keys, private keys, and full config data. Authenticated `POST /v1/api/trust-command` now supports `pin-active-certificate` to persist the active daemon API TLS certificate fingerprint for GUI/pairing TOFU flows and publishes only a redacted trust event. Authenticated peer/folder/discovery config command endpoints now mutate config through the existing CLI configops layer and rely on hot reload for adoption. Authenticated `/v1/transfer-command` now supports runtime-only `pause`/`resume` plus one-shot `cancel` for future folder/peer transfer passes without editing config; observed cancellations publish `transfer.cancelled` and clear their scoped marker. Authenticated `/v1/service-command` now returns reviewable status/start/stop/restart service-manager handoffs for systemd/launchd/Windows without executing privileged host commands. Authenticated `/v1/config` now reads redacted config and applies only non-secret patches, preserving existing generated API keys while rejecting secret-bearing patch fields. Authenticated `/v1/logs` now provides a bounded non-streaming snapshot of recent event/log records for GUI warning/log views without exposing filesystem log files. Authenticated `/v1/identity-package` now exports a pairing package from the current on-disk config, returning public discovery identity plus selected same-identity bootstrap proof material while omitting daemon API keys/private identity keys and keeping events non-secret. Revoked identity records now persist non-secret group/discovery/proof-hash material and import-side pairing rejects packages that match revoked material before proof validation, preventing a compromised identity file from silently reestablishing trust. Revocation planning now also exposes explicit reconnect policy fields requiring a newly generated identity and manual sharing/import on each peer, with auto-rejoin disabled and revoked identity material marked non-reusable. Import-side pairing planning treats that proof material strictly as an introduction/authentication secret, pins that introduction to the level-10 `maximum-high-cpu` profile, explicitly prioritizes bootstrap security over speed/resource cost, requires dedicated per-peer-pair keys before ordinary traffic, and prototype peer-pair key-material generation now creates fresh random traffic keys per peer pair negotiation. Rekey planning handles encryption-level upgrades/downgrades plus scheduled rotation, exposes redacted audit/status fields for previous/current highest levels and change direction, and after replacement key exchange/activation, the key-state helper authorizes only the new key, records the previous key as revoked, and retains redacted lifecycle audit events. The shared identity bootstrap key now has an explicit manual-only lifecycle plan so automatic three-month traffic-key rotation does not apply to it; regeneration remains reserved for GUI/API safety flows. GUI/mobile import UX, configurable rotation policy, animated codes, and full dedicated per-peer-pair key negotiation execution remain planned; the first global identity revocation foundation is now a prototype planner that notifies only reachable identity-derived peers, disconnects identity-derived peers/folders, preserves manual peers/folders, avoids silent bootstrap-key rotation, and requires a new manual identity before reconnect.

## Phase 6 — GUI and remote management

- Latest passive refactor checkpoint: runtime transfer pause/cancel scope tracking now lives in focused `internal/transfercontrol` package with exact, folder-wide, peer-wide, and clear-after-observation scope tests. Authenticated transfer-command response handling now lives in focused `internal/apicontrol` package with pause/resume/cancel acceptance plus validation tests. Authenticated mesh-settings read/queue response handling and secret-redaction guardrails now also live in `internal/apicontrol` with package tests, while optional web GUI command response handling for disabled headless status and trusted local package install has been extracted into the same package. Authenticated service-command handoff response handling now lives in `internal/apicontrol` with package tests for reviewable handoff rendering and invalid request rejection. Authenticated API trust status and trust command handling now live in `internal/apicontrol` with package tests proving pinned-certificate status reporting and active-certificate pinning while preserving generated API keys. Authenticated peer/folder/discovery config command handling now lives in `internal/apicontrol` with package tests proving config mutation, unsupported-action rejection, compact non-secret responses, and hot-reload-adoption messaging. Authenticated identity package export/import response handling now lives in `internal/apicontrol` with package tests proving current-config package export, dedicated peer-pair key preparation, and no secret leakage in import responses. Daemon API request URL/client and TLS trust construction now live in focused `internal/daemonclient` with tests for configured HTTPS trust and certificate fingerprint pinning. Authenticated backup-jobs response handling now lives in `internal/apicontrol` with package tests proving restore-job snapshot filtering plus retention/repair job inclusion. Backup scrub compact API status summarization now lives in `internal/apicontrol` with package tests for archive/checkpoint/repair counters. Authenticated config update response handling and local settings-document mirroring now live in `internal/apicontrol` with package tests proving non-secret patch persistence, secret preservation, and mesh-visible local settings-document updates. `cmd/fse` keeps thin compatibility wrappers for existing command tests.

- Latest daemon monitor/folder projection refactor checkpoint: daemon folder-monitor config projection and hot-reload rebuild logic now live in focused `internal/daemonmonitor` package tests, preserving the safety rule that replacement watchers start before old watchers close and failed replacements keep the existing monitor active while `cmd/fse` only wires runtime callbacks. Daemon folder-sync config projection now lives in focused `internal/daemonfolders` package tests, preserving folder ID/path/sync group/mode/block size/ignore/permission mapping while `cmd/fse` only keeps the runtime compatibility wrapper.

- Latest discovery-control refactor checkpoint: daemon discovery source polling, discovery poll state-update/event-publication, discovered-peer API-state merge, live sidecar/helper endpoint observation projection, stream TCP address parsing, and runtime discovery-source assembly now live in focused `internal/discoverycontrol` package tests. Public-DHT router injection is covered there so `cmd/fse` only supplies the live libp2p router factory. `cmd/fse` now wires those helpers from the daemon runtime instead of carrying the polling/merge/source-assembly implementation inline, and the serious harness covers the extracted discovery-control contract.

- Latest CLI-output refactor checkpoint: CLI error-output rendering now lives in focused `internal/clioutput` with package tests for concise human-readable `fse:` lines, empty-context formatting, and usage-style formatted lines, while stream pull summary rendering, snapshot marker list/single/delete, snapshot restore-plan, restore-result, snapshot-retention, metadata compact/import/split, maintenance scrub/backup-scrub, and quick-index scan output formatting also lives in `internal/clioutput` with package tests for stable rows/summaries; `cmd/fse` keeps thin exit/print wrappers. Top-level CLI parse/config-resolution/runtime dispatch orchestration now lives in focused `internal/cliruntime`, with package tests proving resolved config paths reach the selected command, parse errors do not dispatch, and option-aware commands preserve their parsed options while `cmd/fse` only supplies process-boundary runners and exit formatting. Scan command config loading plus output orchestration now lives in focused `internal/scancli` with package tests for loading the active config, writing metadata-only manifests, and returning stable CLI output, while `cmd/fse` keeps only print/error handling. Stream command config loading now lives in focused `internal/streamcontrol.RunConfigured` with package tests for preserving active config, CLI options, and stdin/stdout handles before serve/pull execution, while `cmd/fse` keeps only pull-summary printing and error handling. Snapshot CLI action selection now lives in focused `internal/snapshotcli` with package tests for list, restore-plan, restore, retention, marker lifecycle, delete output, runner-error propagation, and unsupported actions, while `cmd/fse` keeps only configured runner wiring plus print/error boundaries. Scriptable config init/show plus peer/folder CLI command handling now lives in focused `internal/clicontrol` with package tests for config skeleton creation, redacted config output, add/update/list/remove output, and unsupported-action errors, while `cmd/fse` keeps only stdout/error boundary wrappers. Service render/control CLI orchestration now lives in focused `internal/servicecontrol` with package tests for render and lifecycle handoff option mapping, while `cmd/fse` keeps only print/error wrappers. Daemon API authenticated request execution now lives in focused `internal/daemonclient` with package tests for API-key headers, successful response bodies, non-OK response errors, configured HTTPS trust, and certificate fingerprint pinning, while `cmd/fse` keeps only CLI printing/error boundary wrappers. CLI status/stop daemon API orchestration now lives in focused `internal/daemoncontrol` with package tests for API-key bootstrap, non-loopback TLS asset generation, and `/v1/status`/`/v1/stop` endpoint selection, while `cmd/fse` keeps only output/error wrappers. Metadata import/split rollback operations now live in focused `internal/metadataops` with package tests for JSON-to-durable import, single-Badger-to-per-folder split, rollback backups, and rejecting per-folder targets as split sources, while `cmd/fse` keeps thin CLI wrappers. Metadata compaction orchestration now lives in focused `internal/metadatacontrol` with package tests for configured peer safe-cursor inputs, selected-folder compaction, and missing-folder errors, while `cmd/fse` keeps only config/store loading plus output/error wrappers. CLI quick-index scan orchestration now lives in focused `internal/scancontrol` with package tests for selected-folder metadata-only indexing, state-path reporting, and missing-folder errors, while `cmd/fse` keeps only output/exit wrappers. Config bootstrap resolve/create handling now lives in focused `internal/configbootstrap` with package tests for explicit config generation, executable-adjacent config selection, and first-common-path creation, while `cmd/fse` only supplies the current executable path. Config validation command handling now lives in focused `internal/validatecontrol` with package tests for successful validation and validation-error propagation, while `cmd/fse` keeps only CLI output/error wiring. Prototype stream serve/pull orchestration now lives in focused `internal/streamcontrol` with package coverage for missing configured folders and configured stream command loading, while `cmd/fse` keeps only CLI output/error boundaries.

- Latest command-dispatch refactor checkpoint: top-level CLI command dispatch and the long usage text now live in focused `internal/commanddispatch` with package tests covering every parsed top-level command and usage coverage, while `internal/cliruntime` handles parse/config-resolution orchestration and `cmd/fse` only wires command runners to process-boundary functions.

- Latest backup-control refactor checkpoint: configured backup scrub execution and config/store orchestration now live in focused `internal/backupcontrol` with package tests for archive/checkpoint/repair status plus configured metadata-store loading, while `cmd/fse` keeps thin wrappers for CLI/API handler compatibility and the serious harness covers the package contract.

- Latest metadata-store refactor checkpoint: daemon metadata-store reload identity checks and hot-reload open/preserve/swap lifecycle decisions now live in focused `internal/metadatastore` with package tests proving hot reload only reopens on effective backend or resolved path changes, successful changed-identity reloads return the new store/path/identity, open failures preserve the currently active store/identity, compaction reuses configured metadata paths, and invalid/partial configs fall back to the default JSON state path; `cmd/fse` keeps store assignment, runtime lifecycle, and compatibility-wrapper wiring.

- Latest metadata CLI refactor checkpoint: metadata command action selection now lives in focused `internal/metadatacli` with package tests for compact, import-json, split-badger, runner error propagation, and unsupported action handling; `cmd/fse` now only wires configured metadata runners and prints returned output.

- Latest daemon runtime refactor checkpoint: daemon scan-due runtime transfer handling and daemon monitor event projection now live in focused `internal/daemonruntime` package tests, preserving raw monitor event publication, ordinary monitor events not starting sync work, folder-wide pause/cancel handling, local sync completion/error publication, peer pause/cancel handling, peer sync finished/error publication, warning callback execution, and cooperative-fetch callback execution while `cmd/fse` only wires concrete peer planners/runners/pullers.

- Latest daemon API handler refactor checkpoint: daemon API HTTP server preparation and serve lifecycle selection now live in focused `internal/daemonapi` package tests, preserving disabled-listener no-op behavior, TLS asset bootstrap for non-loopback auto listeners, bootstrap-failure handling, plaintext-vs-TLS serve selection, normal server-closed suppression, and non-close serve error reporting while `cmd/fse` only starts the prepared lifecycle helper. Store-backed API handler registration/refresh now lives in focused `internal/daemonapihandlers`, with package tests proving the maintenance, backup, snapshot/restore/retention, and mesh-settings handlers install together and can be refreshed after metadata-store reloads; startup/static command handler registration and live runtime handler construction now also live in `internal/daemonapihandlers`, with package tests proving peer/folder/discovery/config/trust/identity/filesystem/config-update/service/transfer/web-GUI handlers install as one startup group and that transfer-control plus current-config/web-GUI runtime dependencies are used from the focused handler package while `cmd/fse` only supplies process-boundary closures and registers the returned handler group.

- Latest maintenance CLI refactor checkpoint: maintenance command action selection now lives in focused `internal/maintenancecli` with package tests for scrub output, backup-scrub output, runner-error propagation, and unsupported action errors; `cmd/fse` now only wires configured scrub runners and prints returned output.

- Latest API scrub/refactor checkpoint: maintenance scrub API response summarization now lives in focused `internal/apicontrol` with package tests for aggregate counters, per-folder mode/cursor projection, incomplete-run status, and control-result conversion that preserves scrub execution counters while keeping `cmd/fse` as scrub execution wiring; maintenance scrub warning projection also lives in `internal/apicontrol` with package tests for API warning/event/log fields, quarantine repair status, and matching-folder state updates; scanner inaccessible-file folder warning projection now lives in `internal/apicontrol` with package tests for matching-folder warning counters/recent records, unknown-folder event emission, and no-op behavior; maintenance scrub execution/planning now lives in `internal/maintenancecontrol` with package tests for folder selection, per-folder mode/budget overrides, mismatch reporting, and unknown-folder rejection; snapshot marker, restore-plan/result, and retention response projection now live in `internal/apicontrol` with package tests for API counters, marker metadata, missing block details, and retention progress fields; snapshot API marker/list/create, restore-plan/restore, and retention response orchestration now lives in focused `internal/snapshotapi` with package tests, while `cmd/fse` keeps thin compatibility adapters from daemon scrub and backup/snapshot execution results.

- Latest daemon lifecycle refactor checkpoint: daemon stop-request state/event/log projection now lives in focused `internal/daemonlifecycle` package tests, preserving the stopped API status, user-visible `daemon.stopped` event, and structured log metadata while `cmd/fse` only applies the projection in the runtime loop. Daemon stop-request signal handling now lives in focused `internal/daemonstop` package tests, preserving idempotent stop-handler closure semantics while `cmd/fse` only registers the handler and selects on the signal. Daemon resource cleanup now lives in focused `internal/daemonrun` package tests, preserving bounded monitor close plus API HTTP server shutdown and cleanup-error collection while `cmd/fse` only calls the helper on runtime exit. Daemon recurring poll deadline tracking and due-work callback ordering now live in focused `internal/daemonloop` package tests, preserving first-tick discovery/metadata reconciliation behavior, deadline advancement from the tick time, discovery-before-metadata action order, and callback skipping before deadlines while `cmd/fse` only wires the live side effects. Daemon debounced config-reload adoption/state refresh now lives in focused `internal/daemonreload` package tests, preserving metadata-store reload/preserve semantics, monitor rebuild state, API state refresh, store-backed handler registration, and public reload events while `cmd/fse` only supplies process-boundary logging, metadata opening, monitor creation, and API server callbacks.

- Latest snapshot-control refactor checkpoint: snapshot path resolution, marker creation/lifecycle action handling, configured marker action orchestration, backup archive-intake planning, restore planning/execution wrappers, marker lookup/update, configured-folder lookup helpers, snapshot-list configured config/store orchestration, and snapshot restore/retention configured config/store orchestration now live in focused `internal/snapshotcontrol` package tests, while `cmd/fse` keeps thin compatibility wrappers for existing CLI/API call sites.

- Latest peer API state refactor checkpoint: peer transfer-limit API projection, endpoint network diagnostics, backup snapshot availability projection, folder sync/warning/index plus peer metadata projection, full daemon API-state assembly, and configured-store opening/fallback now live in focused `internal/apistate` package tests for local cap causes, unpromoted container-bridge diagnostics, archive/checkpoint availability mapping, deferred-delete catch-up status, locked-apply warnings, peer metadata cursors, folder index counters, config/store-derived top-level state, and fallback status projection when the configured metadata backend cannot open. Backup checkpoint root resolution now lives in `internal/snapshotcontrol`, while `cmd/fse` only opens the configured metadata store for compatibility call sites and supplies config-relative backup roots.

- Latest endpoint-candidate checkpoint: `internal/routing` now has a tested endpoint-candidate builder that merges manual endpoints, trusted sidecar/helper endpoint observations, and peer-assisted rendezvous hints into a deterministic list. Rendezvous helpers are marked control-plane-only so they can help connection setup without being mistaken for file/block transfer routes, and `sidecar` is now an accepted endpoint kind for trusted container/helper observations. Daemon-scheduled manual HTTP peer pulls now choose from generated endpoint candidates for configured endpoints plus live sidecar/helper observations gathered during daemon discovery polling, preserve endpoint/observation network hints, ignore control-plane-only candidates for data pulls, and can prefer a reachable hinted/local sidecar HTTP route over a configured WAN endpoint. Stream metadata dialing uses the same live observation list for TCP stream endpoints.

- Latest transfer-source intelligence checkpoint: `internal/routing` now provides a tested source-selection planner that filters unreachable or wrong-content candidates, prefers direct peer paths over relay/mesh paths when the same requested content is available, permits relay/mesh only when it is the reachable source, prefers true local/LAN direct endpoints over direct WAN/Internet endpoints for equivalent content, treats explicit VPN/overlay endpoints plus Tailscale CGNAT IPv4 and Tailscale IPv6 ULA addresses as non-local for bandwidth/source-cost decisions, classifies common Docker bridge/NAT endpoint ranges as `container_bridge` rather than true LAN unless explicit local-container-gateway hints, configured `discovery.networkHints.localContainerGatewayIPs`, configured `discovery.networkHints.localCIDRs`, or configured manual endpoint `networkHint: "local"` values promote them, prefers a relay carrier peer that already has requested content over fetching that content through the carrier from a farther peer, and returns stable decision reasons for later status/log surfacing. Runtime cooperative same-LAN block-fetch planning/status rendering, daemon cooperative-fetch event publishing, and peer-pull to runtime diagnostics projection now live in focused `internal/runtimesync` package tests while `cmd/fse` keeps only thin API-event adapters. Peer sync-finished route and missing-ignore-include message formatting now lives in focused `internal/peerevents` package tests while `cmd/fse` keeps thin compatibility wrappers. Metadata reconciliation stream endpoint candidate planning, active metadata-only catch-up decision logic, and runtime metadata TCP stream dialer selection now live in focused `internal/metadatareconcile` package tests while `cmd/fse` keeps runtime observation wiring and event orchestration. Daemon-scheduled HTTP folder pull planning now lives in focused `internal/peerpullplan` package tests while `cmd/fse` keeps only a thin compatibility wrapper for runtime execution. Daemon-scheduled HTTP folder pulls now use local `manual` HTTP endpoints before direct WAN peers, keep explicit `vpn` HTTP endpoints direct but non-local, keep `relay`/`proxy` HTTP endpoints as a fallback only when no direct HTTP peer path is available for the folder pass, include `routePath`, `routeNetwork`, and `routeReason` in `peer.sync.finished` messages for API/GUI diagnostics, and retain all candidate HTTP block sources so manual peer block downloads prefer direct/local block sources over relay sources for the same requested block. Prototype stream pulls can now choose a direct/local stream block source for actual block data instead of using the relay/index stream when equivalent content is directly available. A tested cooperative block-fetch planner now assigns one deterministic WAN fetcher for same-true-LAN peers needing the same Internet-only block, then assigns the remaining local peers to fetch from that LAN source while leaving peers outside the true-local group on independent WAN fetches; daemon live transfer passes now build that cooperative plan for selected direct/local HTTP pulls and publish `transfer.cooperative_block_fetch.planned` so same-LAN fetch coordination is part of runtime scheduling instead of only a routing helper. Peer `/v1/status` entries now expose `networkDiagnostics` with `container_bridge_isolated` guidance when a configured Docker/container bridge endpoint is degraded and has not been promoted by explicit hints, including address, route path/network, and published-port/gateway/CIDR/sidecar remediation guidance. Stream metadata dialing now builds generated endpoint candidates for TCP stream endpoints, including trusted sidecar observations, skips control-plane-only candidates, and prefers reachable local sidecar routes over configured WAN stream endpoints. Peer-assisted NAT traversal now has a tested planner that selects a reachable public helper peer able to reach both sides, skips work when the target is already direct, and models the helper as control-plane direct-session negotiation rather than data relay. A separate control-plane negotiation path planner now finds reachable multi-hop relay/mesh paths for endpoint hints and hole-punch setup while explicitly marking them control-plane-only so they are not used as sync data routes. Broader LAN/VPN/container classification and live NAT negotiation wiring remain planned.

- Latest desktop GUI artifact checkpoint: the desktop scaffold now has concrete Wails source plus an isolated build handoff. The Wails native shell now implements GUI-owned non-service daemon launch/adoption for hosts where no system/user service is installed or reachable: it verifies the bundled engine, starts it as a separate process with isolated config/state and a local encrypted API, persists PID/API/credential/session metadata for reconnect, and stops temporary sessions through authenticated `/v1/stop` rather than in-process control. `scripts/build-desktop-gui-wails.sh <version>` runs selected Wails/Node Docker/Podman builder images with the repo mounted read-only, copies `desktop-gui/` into container scratch space, writes per-target outputs under `desktop-gui/wails-output/<target>/` for Linux, macOS, and Windows on amd64/arm64, supports default/per-OS/per-target builder image selection, and verifies selected images with `docker/podman image inspect` before builds without installing tooling on the Hermes host. `development/desktop-wails-builder/Dockerfile` plus `scripts/build-desktop-wails-builder-image.sh` now provide a project-owned isolated builder-image recipe tagged `fse-desktop-wails-builder:debian12-wails2.10.2` with Go, Node/npm, Wails v2.10.2, Linux WebKit/GTK dependencies, and Windows cross-compiler dependencies while keeping image/cache state outside the source tree. `development/desktop-wails-builder/Dockerfile.linux-arm64` plus the builder-image script's Dockerfile/platform selectors now provide a native ARM64 Linux builder path for runtimes that can run `--platform linux/arm64`; the GUI build script passes `--platform linux/arm64` for that native target and only sets aarch64 cross-compiler/pkg-config variables when the selected image is not ARM64. `development/desktop-wails-builder/Dockerfile.darwin-osxcross` plus `scripts/build-desktop-wails-darwin-builder-image.sh <sdk-tarball>` now provide the selected Apple SDK/osxcross-capable isolated builder layer for darwin-amd64/darwin-arm64: it requires a caller-supplied SDK tarball, stages it only under `/development/fse-desktop-wails-builder-cache/darwin-osxcross-context`, builds `fse-desktop-wails-builder:debian12-wails2.10.2-darwin-osxcross`, removes the temporary SDK copy, and does not download or commit Apple SDK contents. `scripts/package-desktop-linux-installers.sh <version>` now provides a reviewable Linux installer packaging handoff for prebuilt linux-amd64/linux-arm64 Wails outputs plus bundled engine resources, producing `.deb`, `.rpm`, and `.AppImage` artifacts with checksums under `build/<version>/desktop-gui/linux-installers/` when `dpkg-deb`, `rpmbuild`, and `appimagetool` are available in the isolated packaging environment. Its AppImage `AppRun` now supports `--fse-engine-daemon` / `FSE_DESKTOP_APPIMAGE_ENGINE_MODE=1` so AppImage-based service setup can run the bundled engine independently from the GUI and later GUI launches can connect/adopt through the authenticated API. The actual six-target Wails builds and final zipped/installer desktop GUI artifacts remain open until a suitable macOS-capable builder/package toolchain is provided and run.
- Latest desktop Wails build verification checkpoint: the isolated Wails builder produced real linux-amd64 and windows-amd64 desktop GUI outputs with `FSE_DESKTOP_CONTAINER_NETWORK=host` and `FSE_DESKTOP_WAILS_BUILDER_IMAGE=fse-desktop-wails-builder:debian12-wails2.10.2`. The verified Linux output was `desktop-gui/wails-output/linux-amd64/fse-desktop`, a non-empty x86-64 Linux ELF executable (9,385,632 bytes). The verified Windows output was `desktop-gui/wails-output/windows-amd64/fse-desktop.exe`, a non-empty PE32+ x86-64 Windows GUI executable (5.8 MiB, SHA-256 `b4f4896b56cc6d403443106e25b3728b075a58f50a22a1c81fc89f1a7dcc935e`). The first default-network attempt hit npm DNS `EAI_AGAIN`, so target builds should use/fix container networking deliberately. The windows-amd64 build copied a verified output but Docker stayed stuck in container removal afterward, so the wrapper was killed after artifact verification. A later linux-arm64 cross-build attempt reached Wails compile but failed because the locally tagged builder image lacked `aarch64-linux-gnu-gcc`; the native ARM64 Linux builder image was then created and validated, but on this amd64 Docker host the QEMU-emulated `npm install` path hung/ran beyond the pass timeout before producing a linux-arm64 Wails output. The Wails build handoff now detects selected image architecture through `docker/podman image inspect` and refuses native Linux ARM64 target containers on a non-ARM64 host unless `FSE_DESKTOP_ALLOW_EMULATED_TARGET_RUN=1` is set, so future cron passes fail fast instead of hanging in QEMU; use native ARM64 builder hardware or an amd64 cross-builder image for linux-arm64. A Windows ARM64 LLVM-MinGW builder layer now exists at `development/desktop-wails-builder/Dockerfile.windows-arm64-llvm-mingw`, with `scripts/build-desktop-gui-wails.sh` preflighting and exporting `aarch64-w64-mingw32-gcc`/`g++` for `windows/arm64`; the first image-build attempt timed out before Docker reached LLVM-MinGW install, so windows-arm64 output is still not verified. Linux-arm64, Windows arm64, and Darwin amd64/arm64 outputs plus release packages remain open.

- Latest desktop Linux installer artifact checkpoint: the complete 0.1.36-dev Linux desktop installer set is consolidated under `build/0.1.36-dev/desktop-gui/linux-installers/`: amd64 and arm64 `.deb`, `.rpm`, and `.AppImage` artifacts plus `SHA256SUMS`. Verification checked Debian package metadata for amd64/arm64, RPM/AppImage file types including x86-64 and aarch64 AppImage runtimes, and `sha256sum -c SHA256SUMS` for all six artifacts. Darwin Wails outputs remain pending on a legally supplied Apple SDK/osxcross builder.

- Latest desktop Darwin build-handoff checkpoint: `scripts/build-desktop-gui-wails.sh` now explicitly exports osxcross compiler pairs for Darwin Wails builds (`o64-clang`/`o64-clang++` for darwin-amd64 and `oa64-clang`/`oa64-clang++` for darwin-arm64) after the target preflight verifies the selected SDK-backed image. `scripts/build-desktop-wails-darwin-builder-image.sh` stages a caller-supplied Apple SDK only in the isolated `/development` context and now preserves compressed SDK tarball suffixes such as `.tar.xz`, removes staged `*.sdk.tar*` copies through an exit trap even if the container build fails, and lets osxcross infer the version from a version-bearing SDK filename. The desktop release packager refuses to replace existing archives unless every selected target has its required non-empty Wails executable (`fse-desktop`, `fse-desktop.exe`, or `fse-desktop.app/Contents/MacOS/fse-desktop`) plus bundled engine resources, so placeholder/empty GUI outputs cannot be packaged as real desktop artifacts. `FSE_DESKTOP_GUI_RELEASE_TARGETS` can produce explicit reviewable partial packages for already-verified targets, and the 2026-06-07 pass produced/checksum-verified linux-amd64, linux-arm64, windows-amd64, and windows-arm64 desktop GUI zips under `build/0.1.36-dev/desktop-gui-partial-linux-windows/`; final release packaging remains all-six by default. `scripts/check-desktop-darwin-readiness.sh <version>` now deterministically reports the remaining Darwin gate without host tooling: legal Apple SDK tarball, local osxcross builder image, Darwin Wails outputs, bundled Darwin engine resources, and final packaging command. Brandon's staged SDK tarball has now been copied and verified at `/development/apple-sdk/MacOSX.sdk.tar.xz` with SHA-256 `1ebebb06516d4e33f3cb4e50a3351abda9573db4ec816c8502d149db2c1ce8e7` and 49,701 listed SDK entries; the readiness check now reports the SDK gate ready while the Darwin builder image and both Darwin Wails outputs remain missing. A later host-network Darwin builder attempt reached osxcross `./build.sh` but failed because Brandon's macOS 26.5 SDK hit upstream osxcross's unsupported-SDK guard; the isolated Darwin Dockerfile now patches the cloned osxcross build script with a narrow `26.*)` fallback to the Darwin 25-family path before running `./build.sh`, with docs and CI contract coverage. A corrected Docker build for `fse-desktop-wails-builder:debian12-wails2.10.2-darwin-osxcross` should be started after confirming no other file-sync build/container/QEMU/chroot environment is active. The prior long Docker build for `fse-desktop-wails-builder:debian12-wails2.10.2-darwin-osxcross` was running with durable status at `/development/logs/file-sync-desktop-darwin/build-darwin-builder.status`, so next passes must inspect that log/status before starting another build environment.

- Latest desktop identity pairing checkpoint: the first remote-management mesh contract now treats same-identity desktop management as a trusted eventually-consistent mesh: each node owns one canonical settings document, GUI/API edits queue durable per-target pending command records, the config file remains a local import/export surface, and offline instances can be inspected or queued for later adoption through reachable peers before owner-side validation/acknowledgement. The daemon now has a tested persistence/delivery foundation for these per-node settings documents and queued changes: JSON and Badger metadata stores persist owner-keyed settings documents plus target-keyed pending settings-change records, reject owner/document and target/change-ID mismatches, list records deterministically, authenticated `GET /v1/mesh/settings` exposes redacted cached documents with optional node filtering without applying another node's config locally, authenticated `POST /v1/mesh/settings-command` queues non-secret remote settings patches without leaking patch keys or values through queue responses/events, authenticated non-secret `/v1/config` patches mirror the local owner's durable settings document with `source: "local-config"`, stream peers can request `mesh_settings_changes` to receive only queued changes addressed to their authenticated node ID, pulling peers can persist delivered changes locally plus owner-apply them idempotently to the target node's canonical settings document, stale delivered changes fail closed without overwriting a newer owner document and are durably marked failed for GUI/API status, local API queueing rejects spoofed origins that do not match the authenticated daemon node, owner-side apply honors an optional `mesh.authorizedSettingsPeers` allow-list in the target document, pending mesh settings changes retain a compact non-secret audit trail for queue/delivery/owner-apply/ack/failure transitions, the routing layer can plan due retry deliveries across reachable direct or trusted relay/mesh management paths while skipping terminal/not-due/max-attempt records, and a mesh-settings-only stream exchange can apply/acknowledge queued offline edits after reconnect without requiring a folder transfer. The Peers & identity view now exposes the mesh status surface for online, relay-reachable, and offline instances: it refreshes redacted per-node mesh documents, queues non-secret selected-host settings patches through the command endpoint, shows local offline-edit pending-change state until the owner node receives/validates/applies/acknowledges it, and shows pending change status without raw JSON inspection. Management/status/config/pairing/identity-negotiation paths now have a tested planner that prefers direct encrypted control routes but explicitly permits relay/mesh control-plane routes with a user-visible slower-path explanation while keeping bulk file/block transfer on the separate direct-preferred source policy.
- Latest desktop identity pairing checkpoint: the Peers & identity view now presents authenticated daemon-generated identity package export through copyable text and downloadable file entrypoints, builds a QR fallback payload data URL for native/browser QR renderers, generates an animated visual code frame list from the parity/FEC frame contract, parses pasted/uploaded imports locally for readiness, and sends parsed packages to daemon-owned authenticated `/v1/identity-import` execution so bootstrap proof/key preparation stays backend-scoped and API responses return only redacted identifiers/status. The optional web GUI placeholder package now presents matching web identity pairing export/import entrypoints for copyable text, downloadable identity files, pasted/uploaded identity-file import, and daemon-owned import execution against the same authenticated identity API. Full desktop QR image rendering remains a UI-shell follow-on, while animated visual-code framing splits the identity payload into conservative ordered fragments with `sessionId`, `frameIndex`, `frameCount`, `payloadSha256`, `payloadByteLength`, and `frameKind` metadata, adds roughly 30% total-frame parity, can recover missing data frames before verifying the complete reassembled payload for import, exposes scanner progress state that de-duplicates already-seen frames while continuing across subsequent animation loops instead of failing after the first incomplete pass, locks a conservative density profile of 64 payload bytes per frame with large-camera-friendly modules/slow timing for weak cameras and shaky hands, and uses privacy-preserving mixed shards so no single captured animated frame exposes a raw contiguous identity-payload chunk. Full desktop camera scanner UI and mobile camera support remain planned.

- Latest desktop remote folder browsing checkpoint: the Folders view now has live remote folder-tree browsing through the selected remote host's authenticated API. The desktop API client calls authenticated `/v1/filesystem/browse`, the daemon returns directory entries only, the Svelte view can copy a selected readable directory into the folder path form, live browse buttons are disabled with an explicit offline/unreachable message when the selected host is not online/paired, and manual path entry remains available when offline so unreachable hosts do not block typed/pasted paths.

- Latest desktop API certificate trust checkpoint: the selected-host daemon settings view now has API certificate trust controls for TOFU pairing. The desktop API client can fetch `/v1/api/trust`, show the active certificate fingerprint plus `trustedCertificateMatches`, and pin the active certificate through `/v1/api/trust-command` without exposing API keys or TLS private key material.

- Latest mobile GUI architecture checkpoint: `docs/MOBILE_GUI_ARCHITECTURE.md` documents the Android and iOS packaging/runtime stance, and the first `mobile-gui/` scaffold now locks the shared small-screen-first contract before platform-native shells are built. Android uses a bundled daemon behind the local encrypted API, foreground service for active sync, WorkManager for deferred/retryable background work, explicit permissions/battery/network policy handling, secure storage, and degraded status reporting. iOS is treated as policy-constrained: local bundled daemon where allowed, remote/opportunistic client fallback when continuous local sync is not realistic, documented background tasks/app refresh/background URLSession/silent push wake paths, short wake-window metadata/block checkpointing, Keychain-backed secure storage, and explicit degraded status when platform limits prevent daemon-like behavior. The scaffold defines the shared mobile navigation views, local encrypted API-only boundary, remote-management parity, identity pairing import/export plus animated scanner requirement, secure credential-store requirement, cellular sync-disable setting, and degraded-status requirement. Mobile identity pairing entrypoints now cover copyable text, downloadable/share-sheet identity files, QR fallback payloads, pasted pairing codes, uploaded identity files, scanned animated codes, shared identity files, daemon-owned encrypted import readiness, and Android Keystore/iOS Keychain secure storage after import. The shared mobile remote-instance contract now supports direct API endpoint/key, pasted pairing code, uploaded identity file, scanned animated pairing code, and shared identity file onboarding, with automatic same-identity instance population from loaded shared identity snapshots while app state stores only credential references and redacted peer/endpoint metadata. The shared mobile daemon API client plus screen binding now covers folders, peers, discovery, transfers, warnings/logs, maintenance, backups, settings, and identity mesh relay management. Mobile identity mesh relay now models direct encrypted API, identity-mesh-relay, and offline-queued routes so unreachable instances can be inspected/configured through reachable identity peers using durable pending settings changes with pending/applied/failed/acknowledged status.
- Latest mobile animated pairing scanner UI checkpoint: `mobile-gui/src/app/mobileAnimatedPairingScannerScreen.ts` renders the shared scanner state for Android and iOS with collected/required/total frame progress, missing-frame and duplicate-frame details, camera permission/error presentation, secure-storage target, and a clear completion message that pairing is complete while connectivity and authorization continue silently in the background.

- Latest mobile degraded sync status checkpoint: `mobile-gui/src/app/mobileDegradedStatus.ts` now gives Android and iOS shells a shared visible degraded-sync model for iOS policy, battery state, user settings, notification permission, network state, and background refresh restrictions, with action-required flags so mobile UIs can explain platform limits without implying continuous always-on sync.

- Latest mobile background sync checkpoint: Android now has a first background sync scheduler decision contract in `mobile-gui/src/android/androidShell.ts`: active folder sync maps to foreground service/user-visible notification state, deferred metadata catch-up/small transfers/scrub-repair/retry work maps to WorkManager, disabled cellular sync blocks mobile work before scheduling, platform constraints produce degraded reasons, durable checkpoints are required before background work yields, and the Android battery-optimization exemption prompt is requested only when continuous/background sync is enabled while explaining idle-sync risk and surfacing degraded status when the OS refuses or the user declines. iOS now has a scheduling decision plus short-wake execution contract in `mobile-gui/src/ios/iosShell.ts`: metadata catch-up, small pending transfers, resumable block chunks, checkpoint flushes, and retry work select documented wake/work opportunities such as background app refresh, background tasks, background URLSession, silent push wakeups, or foreground-only fallback while honoring cellular/platform-policy blocks; short wake windows carry ordered work priorities, remaining wake budget, early exit deadline, durable checkpoint-before-exit, quick-exit, and next-wake rescheduling requirements.

- Latest mobile cellular sync setting checkpoint: `mobile-gui/src/app/mobileSyncSettings.ts` centralizes the user setting that disables synchronization on mobile/cellular networks; Android and iOS background decisions now use the shared network policy so cellular work is blocked before scheduling while Wi-Fi/Ethernet remain allowed.

- Latest mobile permissions checkpoint: `mobile-gui/src/app/mobilePermissions.ts` provides the shared Android/iOS permission request/status planner for storage, local-network/discovery, location-for-Wi-Fi-state, notifications, foreground/background sync, background processing, and battery optimization needs based on configured sync options. Android and iOS shell states expose `permissionPlan`, and docs explicitly mark this as a request/status contract rather than native entitlement implementation.

- [x] Desktop GUI application. Stack decision is documented in `docs/DESKTOP_GUI_ARCHITECTURE.md`: use Wails + Svelte/TypeScript, keep the GUI and daemon as separate processes, communicate over the authenticated encrypted API, bundle the correct daemon binary per target, and package reproducibly for Windows/macOS/Linux amd64+arm64. The first `desktop-gui/` frontend scaffold locks the separate-process boundary with a Svelte/TypeScript package contract, API-only daemon status client, minimal local-daemon connection shell, bundled-engine resource manifest/checksum verification gate, encrypted lifecycle-control contract, Wails config, and native-shell bridge that reads packaged resource observations and invokes service-command lifecycle actions only after verification. `scripts/package-desktop-engine-resources.sh <version>` now copies the six existing daemon release binaries from `build/<version>/` into the `desktop-gui/resources/engine/` per-target layout and writes resource `manifest.json`/`SHA256SUMS` without building binaries or installing GUI tooling. `scripts/package-desktop-gui-release.sh <version>` now packages already-built Wails outputs plus verified engine resources into one zip per target under `build/<version>/desktop-gui/`, includes `docs-snapshot/`, writes `SHA256SUMS`, and does not run Wails/npm/Go builds or install toolchains. First-launch registration scaffolding now detects current OS daemon registration state through the native shell, asks whether to configure automatic startup/login/start-at-boot, and requests bundled daemon registration only after bundle verification through the encrypted service-command API. The native-shell/frontend contract now includes daemon-owned tray status, startup integration status, and a tray open/focus bridge so later platform implementations can open the separate GUI without keeping it resident. Windows startup/login and tray/menu behavior now has a concrete reviewable bridge contract that uses Service Control Manager ownership, verified encrypted service-command lifecycle status with `platform: 'windows'`, an `fse-desktop://open` protocol-registration handoff, and a `Start-Process 'fse-desktop://open'` tray-open/focus handoff without direct daemon launch. A local engine control panel now reads redacted daemon config and provides authenticated API-only forms for peer, folder, discovery, transfer, maintenance, web GUI, and non-secret config commands, with per-area pending/accepted/failed/completed status. The shell now separates those controls into a host-scoped information architecture with dedicated overview, operational, daemon settings, desktop settings, and help/details views. Remote status hydration, identity pairing UX polish, real native shell implementations, and six-target GUI packaging remain planned.
